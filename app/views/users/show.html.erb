<div class="row">
  <div class="col-md-9" style="text-align: center">
    <div class="container">
        <div id="sort_results" date-rome>
          <%= render 'users/lections_list' %>
        </div>
    </div>
  </div>
  <div class="col-md-3">
    <% if current_user == @user %>
    <%= link_to "Add as a user", {controller: "lections", action: "new", user_id: @user.id }, class: "btn btn-outline-info btn-sm float-right" %>
    <%= render 'users/edit_widget'  %>
    <% end %>
    <div> <%= render 'users/info_widget' %> </div>
    </div>
</div>
